const { ClientError } = require('../../../tools/Error');
const { currentDate, currentTime } = require('../../../tools/Tools');
const { KARDEX_TYPES, KARDEX_MOTIVOS } = require('../../../config/constants');

module.exports = ({ conec }) => async function detailt(data) {
    
}

