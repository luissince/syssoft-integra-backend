services:
  app:
    container_name: my-node-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5002:80"
    volumes:
      - sysintegra-backend:/app/src/path


volumes:
  sysintegra-backend:
